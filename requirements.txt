requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
curl_cffi>=0.7        # TLS-impersonating client; needed to reach Eventim (zappa)
tzdata                # IANA tz database for zoneinfo (Asia/Jerusalem) on all OSes
